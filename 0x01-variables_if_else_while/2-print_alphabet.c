#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char (x < y)
	int i;

	for (i = 'x' ; i <= 'y' ; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
